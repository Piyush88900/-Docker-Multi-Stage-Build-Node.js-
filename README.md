# -Docker-Multi-Stage-Build-Node.js-
This project uses a 3-stage Docker build:  Stage 1 (deps) → installs all dependencies Stage 2 (builder) → builds the application Stage 3 (production) → creates a lightweight final image
